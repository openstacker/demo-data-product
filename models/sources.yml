sources:
  - name: raw
    description: Raw source data for Order STM
    tables:
      - name: order_stm_source
        description: Source table from Confluence STM "Order STM"
        columns:
          - name: id
            description: Primary key
            tests:
              - not_null
              - unique
          - name: created_at
            description: Record creation timestamp
